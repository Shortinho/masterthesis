) %>%
mutate(
variable = vn,
lag_full = lag_full,
max_abs_acf_full = max_full
) %>%
relocate(variable)
list(summary = summary, events = ev_lags)
})
# --- Summary table across variables ---
lag_summary <- bind_rows(lapply(results_list, `[[`, "summary"))
lag_summary
#### second version for CCF ####
library(dplyr)
library(tidyr)
# --- Params ---
upper_q <- 0.90
lower_q <- 0.10
win_L   <- 20   # half-window length in samples
min_sep <- 5    # minimum separation between detected events
# --- Thresholds ---
upper_decile <- quantile(combined$mean_gray, upper_q, na.rm = TRUE)
lower_decile <- quantile(combined$mean_gray, lower_q, na.rm = TRUE)
gs <- combined$mean_gray
# --- Local max/min helpers ---
is_local_max <- function(x) {
nx <- length(x); res <- rep(FALSE, nx)
idx <- 2:(nx-1); res[idx] <- (x[idx] > x[idx-1]) & (x[idx] > x[idx+1])
res
}
is_local_min <- function(x) {
nx <- length(x); res <- rep(FALSE, nx)
idx <- 2:(nx-1); res[idx] <- (x[idx] < x[idx-1]) & (x[idx] < x[idx+1])
res
}
# --- Detect events ---
peaks_idx   <- which(is_local_max(gs) & gs >= upper_decile)
troughs_idx <- which(is_local_min(gs) & gs <= lower_decile)
events_idx  <- sort(c(peaks_idx, troughs_idx))
# --- Enforce minimum separation ---
if (length(events_idx) > 1) {
keep <- logical(length(events_idx)); last_kept <- -Inf
for (i in seq_along(events_idx)) {
if (events_idx[i] - last_kept >= min_sep) {
keep[i] <- TRUE; last_kept <- events_idx[i]
}
}
events_idx <- events_idx[keep]
}
# --- Valid events (respecting window) ---
n <- nrow(combined)
valid <- events_idx[(events_idx - win_L >= 1) & (events_idx + win_L <= n)]
# --- Function: lag per event for one variable ---
lag_from_window_ccf <- function(x, y, lag.max = NULL) {
xz <- scale(x, center = TRUE, scale = TRUE)[,1]
yz <- scale(y, center = TRUE, scale = TRUE)[,1]
res <- ccf(xz, yz, plot = FALSE, lag.max = lag.max)
lag <- res$lag[which.max(abs(res$acf))]
val <- max(abs(res$acf))
c(lag = lag, max_abs_acf = val)
}
vars_to_test <- c("S_Ti", "S", "Ca", "Ti", "Fe_Ti",'Fe','Mn_Ti', 'Mn')
# --- Main loop ---
results_list <- lapply(vars_to_test, function(vn) {
y <- combined[[vn]]
# full-series CCF
ccf_full <- ccf(gs, y, plot = FALSE)
lag_full <- ccf_full$lag[which.max(abs(ccf_full$acf))]
max_full <- max(abs(ccf_full$acf))
# event-based CCFs
ev_lags <- lapply(valid, function(i0) {
idx <- (i0 - win_L):(i0 + win_L)
out <- lag_from_window_ccf(gs[idx], y[idx])
tibble(event_center = i0,
lag_event = as.integer(out["lag"]),
max_abs_acf_event = as.numeric(out["max_abs_acf"]))
}) %>% bind_rows()
summary <- ev_lags %>%
summarise(
n_events = n(),
median_lag_event = median(lag_event, na.rm = TRUE),
iqr_lag_event_low = quantile(lag_event, 0.25, na.rm = TRUE),
iqr_lag_event_high = quantile(lag_event, 0.75, na.rm = TRUE),
mean_abs_acf_event = mean(max_abs_acf_event, na.rm = TRUE)
) %>%
mutate(
variable = vn,
lag_full = lag_full,
max_abs_acf_full = max_full
) %>%
relocate(variable)
list(summary = summary, events = ev_lags, ccf_full = ccf_full)
})
# --- Summary table ---
lag_summary <- bind_rows(lapply(results_list, `[[`, "summary"))
lag_summary
# --- Plots ---
par(mfrow = c(2, 1)) # two plots per variable
for (vn in vars_to_test) {
res <- results_list[[which(vars_to_test == vn)]]
# Full-series CCF plot
plot(res$ccf_full, main = paste("Full-series CCF: mean_gray &", vn))
# Event lag histogram
hist(res$events$lag_event,
breaks = 30,
main = paste("Event-wise lag distribution:", vn),
xlab = "Lag (samples)", col = "gray")
}
par(mfrow = c(1,1))
vars_to_test <- c("S_Ti", "S", "Ca", "Ti", "Fe_Ti",'Fe','Mn_Ti', 'Mn')
# --- Main loop ---
results_list <- lapply(vars_to_test, function(vn) {
y <- combined[[vn]]
# full-series CCF
ccf_full <- ccf(gs, y, plot = FALSE)
lag_full <- ccf_full$lag[which.max(abs(ccf_full$acf))]
max_full <- max(abs(ccf_full$acf))
# event-based CCFs
ev_lags <- lapply(valid, function(i0) {
idx <- (i0 - win_L):(i0 + win_L)
out <- lag_from_window_ccf(gs[idx], y[idx])
tibble(event_center = i0,
lag_event = as.integer(out["lag"]),
max_abs_acf_event = as.numeric(out["max_abs_acf"]))
}) %>% bind_rows()
summary <- ev_lags %>%
summarise(
n_events = n(),
median_lag_event = median(lag_event, na.rm = TRUE),
iqr_lag_event_low = quantile(lag_event, 0.25, na.rm = TRUE),
iqr_lag_event_high = quantile(lag_event, 0.75, na.rm = TRUE),
mean_abs_acf_event = mean(max_abs_acf_event, na.rm = TRUE)
) %>%
mutate(
variable = vn,
lag_full = lag_full,
max_abs_acf_full = max_full
) %>%
relocate(variable)
list(summary = summary, events = ev_lags, ccf_full = ccf_full)
})
# --- Summary table ---
lag_summary <- bind_rows(lapply(results_list, `[[`, "summary"))
lag_summary
# --- Plots ---
par(mfrow = c(2, 1)) # two plots per variable
for (vn in vars_to_test) {
res <- results_list[[which(vars_to_test == vn)]]
# Full-series CCF plot
plot(res$ccf_full, main = paste("Full-series CCF: mean_gray &", vn))
# Event lag histogram
hist(res$events$lag_event,
breaks = 20,
main = paste("Event-wise lag distribution:", vn),
xlab = "Lag (samples)", col = "gray")
}
par(mfrow = c(1,1))
#### descriptive stats by facies ###############################################
vars_to_summarize <- c(
# closed-sum elements
'Fe', 'Mn', 'S', 'Ti', 'Al', 'Si', 'K', 'Rb', 'Zr', 'Ca', 'Sr', 'P', 'Ba',
# centered log ratio elements
'Fe.y', 'Mn.y', 'S.y', 'Ti.y', 'Al.y', 'Si.y', 'K.y', 'Rb.y', 'Zr.y', 'Ca.y', 'Sr.y', 'P.y', 'Ba.y',
# raw counts
'Fe.x', 'Mn.x', 'S.x', 'Ti.x', 'Al.x', 'Si.x', 'K.x', 'Rb.x', 'Zr.x', 'Ca.x', 'Sr.x', 'P.x', 'Ba.x',
# ratios
'Mn_Fe', 'S_Ti', 'Fe_Ti', 'Si_Ti', 'Ti_Al', 'Ba_Ti', 'Ca_Ti',
# CNS
'TNWt', 'TCWt', 'TSWt',
# HSI
'RABD673', 'Moving.Average',
# grayscale
'mean_gray'
)
vars_to_test <- vars_to_summarize
test_results <- lapply(vars_to_test, function(var) {
formula <- as.formula(paste(var, "~ facies_class"))
result <- wilcox.test(formula, data = combined_df)
tibble(variable = var, p_value = result$p.value, method = result$method)
})
test.df <- bind_rows(test_results) # small p-values indicate difference in distributions
test.df
combined_df <- read_csv('data/generated/combined/combined_top_200_inc_coh.csv')
test_results <- lapply(vars_to_test, function(var) {
formula <- as.formula(paste(var, "~ facies_class"))
result <- wilcox.test(formula, data = combined_df)
tibble(variable = var, p_value = result$p.value, method = result$method)
})
test.df <- bind_rows(test_results) # small p-values indicate difference in distributions
test.df
print(test.df, n= 62)
print(test.df$p_value)
cat(test.df$p_value)
vars_to_test <- c("S_Ti", "S", "Ca", "Ti", "Fe_Ti",'Fe','Mn_Ti', 'Mn', 'Ba_Ti', 'Ba')
combined <- compute_ratios(combined, 'Mn', 'Fe')
combined <- compute_ratios(combined, 'Ba.x', 'Ti.x')
vars_to_test <- c("S_Ti", "S", "Ca", "Ti", "Fe_Ti",'Fe','Mn_Ti', 'Mn', 'Ba_Ti', 'Ba')
# --- Main loop ---
results_list <- lapply(vars_to_test, function(vn) {
y <- combined[[vn]]
# full-series CCF
ccf_full <- ccf(gs, y, plot = FALSE)
lag_full <- ccf_full$lag[which.max(abs(ccf_full$acf))]
max_full <- max(abs(ccf_full$acf))
# event-based CCFs
ev_lags <- lapply(valid, function(i0) {
idx <- (i0 - win_L):(i0 + win_L)
out <- lag_from_window_ccf(gs[idx], y[idx])
tibble(event_center = i0,
lag_event = as.integer(out["lag"]),
max_abs_acf_event = as.numeric(out["max_abs_acf"]))
}) %>% bind_rows()
summary <- ev_lags %>%
summarise(
n_events = n(),
median_lag_event = median(lag_event, na.rm = TRUE),
iqr_lag_event_low = quantile(lag_event, 0.25, na.rm = TRUE),
iqr_lag_event_high = quantile(lag_event, 0.75, na.rm = TRUE),
mean_abs_acf_event = mean(max_abs_acf_event, na.rm = TRUE)
) %>%
mutate(
variable = vn,
lag_full = lag_full,
max_abs_acf_full = max_full
) %>%
relocate(variable)
list(summary = summary, events = ev_lags, ccf_full = ccf_full)
})
# --- Summary table ---
lag_summary <- bind_rows(lapply(results_list, `[[`, "summary"))
lag_summary
# --- Plots ---
par(mfrow = c(2, 1)) # two plots per variable
for (vn in vars_to_test) {
res <- results_list[[which(vars_to_test == vn)]]
# Full-series CCF plot
plot(res$ccf_full, main = paste("Full-series CCF: mean_gray &", vn))
# Event lag histogram
hist(res$events$lag_event,
breaks = 20,
main = paste("Event-wise lag distribution:", vn),
xlab = "Lag (samples)", col = "gray")
}
View(combined)
vars_to_test <- c("S_Ti", "S", "Ca",'Ca_Ti' "Ti", "Fe_Ti",'Fe','Mn_Ti', 'Mn', 'Ba_Ti', 'Ba')
vars_to_test <- c("S_Ti", "S", "Ca",'Ca_Ti', "Ti", "Fe_Ti",'Fe','Mn_Ti', 'Mn', 'Ba_Ti', 'Ba')
# --- Main loop ---
results_list <- lapply(vars_to_test, function(vn) {
y <- combined[[vn]]
# full-series CCF
ccf_full <- ccf(gs, y, plot = FALSE)
lag_full <- ccf_full$lag[which.max(abs(ccf_full$acf))]
max_full <- max(abs(ccf_full$acf))
# event-based CCFs
ev_lags <- lapply(valid, function(i0) {
idx <- (i0 - win_L):(i0 + win_L)
out <- lag_from_window_ccf(gs[idx], y[idx])
tibble(event_center = i0,
lag_event = as.integer(out["lag"]),
max_abs_acf_event = as.numeric(out["max_abs_acf"]))
}) %>% bind_rows()
summary <- ev_lags %>%
summarise(
n_events = n(),
median_lag_event = median(lag_event, na.rm = TRUE),
iqr_lag_event_low = quantile(lag_event, 0.25, na.rm = TRUE),
iqr_lag_event_high = quantile(lag_event, 0.75, na.rm = TRUE),
mean_abs_acf_event = mean(max_abs_acf_event, na.rm = TRUE)
) %>%
mutate(
variable = vn,
lag_full = lag_full,
max_abs_acf_full = max_full
) %>%
relocate(variable)
list(summary = summary, events = ev_lags, ccf_full = ccf_full)
})
# --- Main loop ---
results_list <- lapply(vars_to_test, function(vn) {
y <- combined_df[[vn]]
# full-series CCF
ccf_full <- ccf(gs, y, plot = FALSE)
lag_full <- ccf_full$lag[which.max(abs(ccf_full$acf))]
max_full <- max(abs(ccf_full$acf))
# event-based CCFs
ev_lags <- lapply(valid, function(i0) {
idx <- (i0 - win_L):(i0 + win_L)
out <- lag_from_window_ccf(gs[idx], y[idx])
tibble(event_center = i0,
lag_event = as.integer(out["lag"]),
max_abs_acf_event = as.numeric(out["max_abs_acf"]))
}) %>% bind_rows()
summary <- ev_lags %>%
summarise(
n_events = n(),
median_lag_event = median(lag_event, na.rm = TRUE),
iqr_lag_event_low = quantile(lag_event, 0.25, na.rm = TRUE),
iqr_lag_event_high = quantile(lag_event, 0.75, na.rm = TRUE),
mean_abs_acf_event = mean(max_abs_acf_event, na.rm = TRUE)
) %>%
mutate(
variable = vn,
lag_full = lag_full,
max_abs_acf_full = max_full
) %>%
relocate(variable)
list(summary = summary, events = ev_lags, ccf_full = ccf_full)
})
# --- Summary table ---
lag_summary <- bind_rows(lapply(results_list, `[[`, "summary"))
lag_summary
# --- Plots ---
par(mfrow = c(2, 1)) # two plots per variable
for (vn in vars_to_test) {
res <- results_list[[which(vars_to_test == vn)]]
# Full-series CCF plot
plot(res$ccf_full, main = paste("Full-series CCF: mean_gray &", vn))
# Event lag histogram
hist(res$events$lag_event,
breaks = 20,
main = paste("Event-wise lag distribution:", vn),
xlab = "Lag (samples)", col = "gray")
}
log(432782/185276)
# --- Thresholds ---
upper_decile <- quantile(combined$mean_gray, upper_q, na.rm = TRUE)
lower_decile <- quantile(combined$mean_gray, lower_q, na.rm = TRUE)
gs <- combined$mean_gray
upper_decile
upper_decile
class(upper_decile)
upper_decile*3
mask_upper <- gs >= upper_decile
mask_upper
mask_lower <- gs <= lower_decile
upper.df <- combined %>% filter(mask_upper)
sum(upper.df)
sum(mask_upper)
lower.df <- combined %>% filter(mask_lower)
s.low <- scale(lower.df)
s.low
View(s.low)
View(lower.df)
s.low %>% mean()
s.low
s.low <- tibble(s.low)
s.low <- scale(lower.df)
class(s.low)
View(s.low)
names(s.low)
mean(s.low['Fe.x'])
mean(s.low[,3])
combined.scaled <- scale(combined)
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
mean()
combined.scaled <- scale(combined) %>% data.frame
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
mean()
centers.upper
combined.scaled <- scale(combined) %>% data.frame()
View(combined.scaled)
combined.scaled$Mn.x %>% sd()
?mean()
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
rowMeans()
centers.upper
View(centers.upper)
class(centers.upper)
names(centers.upper)
clr_sel <- c('Fe.y', 'Mn.y', 'S.y', 'Ti.y', 'Al.y', 'Si.y', 'K.y', 'Rb.y', 'Zr.y', 'Ca.y', 'Sr.y', 'P.y', 'Ba.y')
centers.upper <- combined.scaled %>%
select(any_of(clr_sel)) %>%
filter(mask_upper) %>%
rowMeans()
centers.upper
clr_sel <- c('Fe.y', 'Mn.y', 'S.y', 'Ti.y', 'Al.y', 'Si.y', 'K.y', 'Rb.y', 'Zr.y', 'Ca.y', 'Sr.y', 'P.y', 'Ba.y')
combined.scaled <- scale(combined) %>% data.frame()
centers.upper <- combined.scaled %>%
select(any_of(clr_sel)) %>%
filter(mask_upper) %>%
rowMeans()
centers.upper
names(combined.scaled)
centers.upper <- combined.scaled %>%
select(any_of(clr_sel)) %>%
filter(mask_upper) %>%
rowMeans()
combined.scaled %>% select(clr_sel)
combined.scaled %>% select(any_of(clr_sel))
mask_upper
centers.upper <- combined.scaled %>%
select(any_of(clr_sel)) %>%
filter(mask_upper) %>%
rowMeans()
clr_sel <- c('Fe.y', 'Mn.y', 'S.y', 'Ti.y', 'Al.y', 'Si.y', 'K.y', 'Rb.y', 'Zr.y', 'Ca.y', 'Sr.y', 'P.y', 'Ba.y')
combined.scaled <- scale(combined) %>% data.frame()
centers.upper <- combined.scaled %>%
select(any_of(clr_sel)) %>%
filter(mask_upper) %>%
rowMeans()
combined.scaled %>%
select(any_of(clr_sel)) %>%
filter(mask_upper) %>%
rowMeans()
combined.scaled %>%
select(any_of(clr_sel)) %>%
filter(mask_upper)
combined.scaled %>%
select(any_of(clr_sel))
combined.scaled %>%
select(any_of(clr_sel)) %>% ncol()
combined.scaled %>%
select(any_of(clr_sel)) %>% nrow()
centers.upper <- combined.scaled %>%
ilter(mask_upper) %>%
select(any_of(clr_sel)) %>%
rowMeans()
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
select(any_of(clr_sel)) %>%
rowMeans()
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
rowMeans() %>%
select(any_of(clr_sel))
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
rowMeans() %>%
select(any_of(clr_sel))
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
rowMeans() %>%
data.frame()
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
select(any_of(clr_sel)) %>%
as.matrix() %>%
rowMeans()
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
summarise(across(all_of(clr_sel), mean, na.rm = TRUE))
centers.lower <- combined.scaled %>%
filter(mask.lower) %>%
summarise(across(all_of(clr_sel), mean))
centers.lower <- combined.scaled %>%
filter(mask_lower) %>%
summarise(across(all_of(clr_sel), mean))
col_pal <- brewer.pal(11, 'BrBG')
col_pal
p1 <- ggplot(centers.lower,
aes(x = center_value, y = fct_rev(variable))) +
geom_col(fill = '#543005') +
scale_x_continuous(limits = x_limits) +
labs(title = "Cluster 1", x = "Cluster Center (Scaled)", y = "Variable") +
theme_minimal()
# Compute column means for upper/lower masks
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
summarise(across(all_of(clr_sel), mean, na.rm = TRUE)) %>%
mutate(group = "Upper")
centers.lower <- combined.scaled %>%
filter(mask_lower) %>%
summarise(across(all_of(clr_sel), mean, na.rm = TRUE)) %>%
mutate(group = "Lower")
# Combine and pivot longer
centers_long <- bind_rows(centers.upper, centers.lower) %>%
pivot_longer(-group, names_to = "variable", values_to = "center_value")
# Plot
ggplot(centers_long, aes(x = center_value, y = fct_rev(variable), fill = group)) +
geom_col(position = "dodge") +
labs(title = "Upper vs Lower Decile Means",
x = "Mean (Scaled)",
y = "Variable") +
theme_minimal()
# --- Compute means ---
centers.upper <- combined.scaled %>%
filter(mask_upper) %>%
summarise(across(all_of(clr_sel), mean, na.rm = TRUE)) %>%
mutate(group = "Upper")
centers.lower <- combined.scaled %>%
filter(mask_lower) %>%
summarise(across(all_of(clr_sel), mean, na.rm = TRUE)) %>%
mutate(group = "Lower")
# --- Pivot to long format ---
centers.upper.long <- centers.upper %>%
pivot_longer(-group, names_to = "variable", values_to = "center_value")
centers.lower.long <- centers.lower %>%
pivot_longer(-group, names_to = "variable", values_to = "center_value")
# --- Define x limits (optional: same for both plots) ---
x_limits <- range(c(centers.upper.long$center_value, centers.lower.long$center_value))
# --- Plot Upper ---
p_upper <- ggplot(centers.upper.long, aes(x = center_value, y = fct_rev(variable))) +
geom_col(fill = "#DFC27D") +
scale_x_continuous(limits = x_limits) +
labs(title = "Upper Decile Means", x = "Mean (Scaled)", y = "Variable") +
theme_minimal()
# --- Plot Lower ---
p_lower <- ggplot(centers.lower.long, aes(x = center_value, y = fct_rev(variable))) +
geom_col(fill = "#543005") +
scale_x_continuous(limits = x_limits) +
labs(title = "Lower Decile Means", x = "Mean (Scaled)", y = "Variable") +
theme_minimal()
p_upper
p_lower
library(dplyr)
library(tidyr)
library(ggplot2)
library(forcats)
View(centers.upper.long)
